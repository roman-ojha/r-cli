import { TabsState } from "./types";

const tabState: TabsState = {
  activeTabId: 1,
  activeTabIndex: 0,
  tabs: [
    {
      id: 1,
      name: "R-CLI",
      program: "r-cli",
    },
    {
      id: 2,
      name: "R-CLI",
      program: "r-cli",
    },
  ],
};

export default tabState;
