import { TabsState } from "./types";

const tabState: TabsState = {
  activeTab: 1,
  numberOfTabs: 2,
  tabs: [
    {
      name: "R-CLI 1",
      location: "cloud",
    },
    {
      name: "R-CLI 2",
      location: "cloud",
    },
  ],
};

export default tabState;
